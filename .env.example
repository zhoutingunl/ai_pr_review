# 复制为 .env 并填入真实值（.env 已加入 .gitignore，禁止提交）

# GitHub 访问令牌（缺省时回落到 `gh auth token`）
GITHUB_TOKEN=

# GitHub Webhook 签名密钥（与 GitHub 仓库 Webhook 配置中的 Secret 一致）
WEBHOOK_SECRET=

# Hermes AI 网关地址（本系统所有 AI 能力都经它）。
# 留空则回落到内置默认（公司内网 http://10.210.32.30:8787，仅 VPN 可达）。
# 公网 / 自建复刻：部署 Hermes + WebUI 后填你自己的 HTTPS 端点，详见 docs/public.md。
HERMES_BASE=
